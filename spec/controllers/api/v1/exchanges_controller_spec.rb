require 'rails_helper'

RSpec.describe Api::V1::ExchangesController, type: :controller do
  describe "POST#create" do
    xit "adds a new open exchange to the db" do
    end
    xit "returns the new exchange as json" do
    end
    xit "does not add incomplete info to the db" do
    end
    xit "does not add incorrect info to the db" do
    end
    xit "returns validation error json" do
    end
  end

  describe "GET#index" do
    xit "returns successful response code and json content" do
    end
    xit "returns all open exchanges in the db for that particular user" do
    end
  end

  describe "GET#show" do
    xit "returns successful response code and json content" do
    end

    xit "returns correct exchange data" do
    end
  end
end
